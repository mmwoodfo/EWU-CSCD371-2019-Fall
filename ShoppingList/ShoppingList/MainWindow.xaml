<!-- Color reference: https://www.designwizard.com/wp-content/uploads/2019/03/colorCombination_60.jpg -->
    <Window x:Class="ShoppingList.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ShoppingList"
        mc:Ignorable="d"
        MinHeight="350"
        MinWidth="650"
        TextElement.FontSize="20"
        d:DataContext="{d:DesignInstance Type=local:MainWindowViewModel}"
        Title="Shopping List" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="0.7*"/>
            <ColumnDefinition Width="0.5*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Column="1"/>
        <StackPanel Grid.Column="2"/>

    <!-- Column 0 LIST BOX -->
        <!--R0-R*-->
        <ListBox 
            ItemsSource="{Binding ShoppingList}" 
            SelectedItem="{Binding SelectedItem}"
            Grid.Column="0" Grid.RowSpan="{Binding SpanAll}"
            SelectionMode="Single" ScrollViewer.HorizontalScrollBarVisibility="Auto" 
            Background="{Binding Tomato}" Foreground="White" FontFamily="Ink Free">

            <ListBox.ItemTemplate>
                <DataTemplate DataType="{x:Type local:Item}">
                    <StackPanel>
                        <TextBlock Text="{Binding Name}" TextWrapping="Wrap"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>

        </ListBox>

    <!-- Column 1 LIST CONTROLS -->
        <!--R0-->
        <TextBox Text="{Binding TextToAddToList}" 
                 Grid.Column="1"
                 Visibility="{Binding AddItemControls}" 
                 TextWrapping="Wrap" SelectionBrush="{Binding Sandy}" Foreground="{Binding SlateGray}" Margin="{Binding Margins}"/>

        <Button Content="Delete Item" 
                Command="" Grid.Column="1" Grid.Row="0" Grid.ColumnSpan="{Binding SpanAll}" 
                Visibility="{Binding SelectedItemControls}" 
                Foreground="{Binding SlateGray}" Background="{Binding Tomato}" Margin="{Binding Margins}"/>
        
        <!--R1-->
        <TextBox Text="{Binding TextToAddToList}" 
                 Grid.Column="1" Grid.Row="1" 
                 Visibility="{Binding SelectedItemControls}" 
                 TextWrapping="Wrap" SelectionBrush="{Binding Sandy}" Foreground="{Binding SlateGray}" Margin="{Binding Margins}"/>

        <!-- Column 2 LIST CONTROLS -->
        <!--R0-->
        <Button Content="Add To List" 
                Command="{Binding AddItemCommand}" 
                Grid.Column="2" Grid.ColumnSpan="{Binding SpanAll}" 
                Visibility="{Binding AddItemControls}" 
                Foreground="{Binding Sandy}" Background="{Binding SlateGray}" Margin="{Binding Margins}"/>

        <!--R1-->
        <Button Content="Update Item" 
                Command="" 
                Grid.Column="2" Grid.Row="1" Grid.ColumnSpan="{Binding SpanAll}" 
                Visibility="{Binding SelectedItemControls}" 
                Foreground="{Binding Sandy}" Background="{Binding SlateGray}" Margin="{Binding Margins}"/>


    </Grid>
</Window>
